"use client";
import { useRef, useState, type FormEvent } from "react";
import { ArrowRight, Check, CheckCircle2, ChevronDown, Clock3, Droplets, Flame, House, MapPin, Menu, Phone, Snowflake, Wrench, X, Zap } from "lucide-react";
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group";
import { Progress } from "@/components/ui/progress";
import { Checkbox } from "@/components/ui/checkbox";
const services = [
 {name:"AC & Cooling",icon:Snowflake,text:"Keep your cool, even on the hottest Virginia days.",details:"Air conditioning diagnostics, repairs, seasonal tune-ups, and replacement options."},
 {name:"Heating",icon:Flame,text:"A warmer home. A little more peace of mind.",details:"Furnace and heat-pump troubleshooting, maintenance, and system replacement."},
 {name:"Plumbing",icon:Droplets,text:"From a small drip to a bigger fix, start here.",details:"Leaking faucets, running toilets, clogged drains, and household pipe repairs."},
 {name:"Water Heaters",icon:Wrench,text:"Bring comfort back to your everyday routine.",details:"Traditional and tankless water-heater repairs, maintenance, and installation."}
];
const initial={service:"AC & Cooling",zip:"",urgency:"This week",name:"",email:"",phone:"",notes:""};
export default function Home(){
 const [menu,setMenu]=useState(false),[step,setStep]=useState(1),[data,setData]=useState(initial),[consent,setConsent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(""),[receipt,setReceipt]=useState("");
 const key=useRef(""),heading=useRef<HTMLHeadingElement>(null);
 const update=(field:keyof typeof data,value:string)=>setData(p=>({...p,[field]:value}));
 const estimate=(service?:string)=>{if(service)update("service",service);setMenu(false);document.getElementById("estimate")?.scrollIntoView({behavior:"smooth"});};
 const next=(n:number)=>{setStep(n);setError("");setTimeout(()=>heading.current?.focus(),0);};
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(step<3){next(step+1);return;}if(!consent){setError("Please confirm that this is a demo request.");return;}setBusy(true);setError("");if(!key.current)key.current=crypto.randomUUID();try{const r=await fetch("/api/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,consent,id:key.current})});const result=await r.json() as {id:string;error?:string};if(!r.ok)throw new Error(result.error||"Your request could not be saved. Please try again.");setReceipt(result.id);setTimeout(()=>heading.current?.focus(),0);}catch(e){setError(e instanceof Error?e.message:"Connection interrupted. Please try again.");}finally{setBusy(false);}}
 function download(){const blob=new Blob([`ComfortPro — FICTIONAL DEMO REQUEST\nReference: ${receipt}\nService: ${data.service}\nZIP: ${data.zip}\nTiming: ${data.urgency}\nName: ${data.name}\nEmail: ${data.email}\nPhone: ${data.phone}\nNotes: ${data.notes}\n\nSaved as a demo only. No appointment or service has been scheduled.`],{type:"text/plain"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`comfortpro-${receipt.slice(0,8)}.txt`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
 return <><div className="demo-banner"><span>CONCEPT WEBSITE</span> Fictional business. Explore the experience — no real services or bookings.</div>
 <header><div className="container nav"><a className="brand" href="#home" aria-label="ComfortPro home"><span className="brand-icon"><House size={25}/></span><span>Comfort<span className="orange">Pro</span><small>HVAC & PLUMBING</small></span></a><nav aria-label="Main navigation" className={menu?"nav-links open":"nav-links"}><a href="#services" onClick={()=>setMenu(false)}>Our services</a><a href="#approach" onClick={()=>setMenu(false)}>The ComfortPro way</a><a href="#areas" onClick={()=>setMenu(false)}>Service area</a><a href="#faq" onClick={()=>setMenu(false)}>FAQs</a></nav><button className="button small nav-estimate" onClick={()=>estimate()}>Get a Free Estimate <ArrowRight size={17}/></button><button className="menu" aria-label={menu?"Close menu":"Open menu"} aria-expanded={menu} onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></div></header>
 <main id="home"><section className="hero"><div className="container hero-grid"><div className="hero-copy"><div className="eyebrow light"><span/> YOUR HOME. YOUR COMFORT.</div><h1>Comfort restored.<br/>Problems <em>solved.</em></h1><p>Heating, cooling & plumbing.<br/>One simple place to start feeling at home again.</p><div className="hero-actions"><button className="button" onClick={()=>estimate()}>Get a Free Estimate <ArrowRight size={19}/></button><a className="button outline" href="tel:+17035550148"><Phone size={17}/> Call Now</a></div><div className="hero-note"><MapPin size={15}/> Northern Virginia <span>•</span> Demo phone: (703) 555-0148</div><div className="hero-promise"><span><Check size={16}/> A clear next step</span><span><Check size={16}/> An easier home-service experience</span></div></div><div className="hero-image"><img src="/technician.webp" alt="Concept image of a technician beside an air conditioner at a brick home" width="1536" height="1024" fetchPriority="high"/><span className="image-caption">AI-generated concept imagery</span><div className="image-card"><span className="round-icon"><House/></span><div>Good to be home.<small>Comfort for every season.</small></div></div></div></div></section>
 <div className="benefits"><div className="container benefits-grid"><div><Snowflake/><span>Cooling & heating<small>Comfort, all year round</small></span></div><div><Droplets/><span>Everyday plumbing<small>Small fixes to fresh starts</small></span></div><div><Clock3/><span>Simple scheduling<small>Tell us what works for you</small></span></div><div><CheckCircle2/><span>Clear communication<small>Know your next step</small></span></div></div></div>
 <section id="services" className="section container"><div className="section-top"><div><div className="eyebrow">WHAT CAN WE HELP WITH?</div><h2>One home. Every kind of comfort.</h2></div><p>Explore the services in this concept.<br/>Choose one to try the estimate experience.</p></div><div className="service-grid">{services.map((s,i)=><article className="service-card" key={s.name}><div className="service-top"><span className="service-icon"><s.icon size={27}/></span><span className="number">0{i+1}</span></div><h3>{s.name}</h3><p>{s.text}</p><details><summary>What’s included <ChevronDown size={16}/></summary><p>{s.details}</p></details><button className="text-button" onClick={()=>estimate(s.name)}>Get an estimate <ArrowRight size={17}/></button></article>)}</div><div className="urgent"><Zap size={22}/><p><strong>Something can’t wait?</strong> Select “As soon as possible” in the demo form to try an urgent request.</p><button onClick={()=>{update("urgency","As soon as possible");estimate();}}>Try urgent request <ArrowRight size={16}/></button></div></section>
 <section id="approach" className="approach"><div className="container approach-grid"><div><div className="eyebrow">THE COMFORTPRO WAY</div><h2>Less hassle.<br/>More feeling at home.</h2><p>A home-service experience should be straightforward, from the first question to the next step. Here’s the journey this concept is built around.</p><a className="text-button" href="#estimate">Let’s get started <ArrowRight size={18}/></a></div><div className="steps">{[["01","Tell us what’s going on","Choose a service and share a few details about your home."],["02","Find your next step","Let us know where you are and when you need help."],["03","Leave the details with us","Try the demo submission and receive your request reference."]].map(([n,t,p])=><div className="step" key={n}><span>{n}</span><div><h3>{t}</h3><p>{p}</p></div></div>)}</div></div></section>
 <section id="estimate" className="section container estimate-grid"><div className="estimate-copy"><div className="eyebrow">LET’S TAKE CARE OF IT</div><h2>Your comfort<br/>starts here.</h2><p>Tell us a little about what you need.<br/>Three quick steps. One clear next move.</p><ul><li><CheckCircle2/> Choose your service</li><li><CheckCircle2/> Tell us where and when</li><li><CheckCircle2/> Submit your demo request</li></ul><div className="demo-explainer"><strong>A real form. A fictional business.</strong><p>Requests are saved for this demo. Use sample contact details. No email is sent and no service is scheduled.</p></div></div><div className="estimate-card">{receipt?<div className="success" role="status"><CheckCircle2 size={48}/><div className="eyebrow">DEMO REQUEST SAVED</div><h3 tabIndex={-1} ref={heading}>You’re all set, {data.name.split(" ")[0]}.</h3><p>Your {data.service.toLowerCase()} demo request has been received and saved.</p><div className="receipt">Reference <strong>{receipt.slice(0,8).toUpperCase()}</strong><span>{data.zip} · {data.urgency}</span></div><p className="fine">This is a demo confirmation. No appointment is booked, and nobody will contact you.</p><button className="button" onClick={download}>Download confirmation</button><button className="text-button" onClick={()=>{setReceipt("");key.current="";setData(initial);setConsent(false);next(1);}}>Try another request</button></div>:<form onSubmit={submit}><div className="form-top"><span>GET A FREE ESTIMATE</span><span>Step {step} of 3</span></div><Progress value={step/3*100} aria-label={`Step ${step} of 3`}/><h3 ref={heading} tabIndex={-1}>{step===1?"What can we help with?":step===2?"Where and when?":"How can we reach you?"}</h3><p className="form-sub">{step===1?"Choose the service that best fits your needs.":step===2?"Enter a ZIP code and your preferred timing.":"Use sample details to test the full experience."}</p>{step===1&&<RadioGroup value={data.service} onValueChange={v=>update("service",v)} aria-label="Service" className="service-options">{services.map(s=><label key={s.name} className={data.service===s.name?"choice selected":"choice"}><s.icon size={22}/><span>{s.name}</span><RadioGroupItem value={s.name} aria-label={s.name}/></label>)}</RadioGroup>}{step===2&&<><label className="field">ZIP code<input value={data.zip} onChange={e=>update("zip",e.target.value)} placeholder="e.g. 20120" inputMode="numeric" autoComplete="postal-code" pattern="[0-9]{5}" maxLength={5} required title="Enter a five-digit ZIP code"/></label><span className="field-label">When would you like help?</span><RadioGroup aria-label="Preferred timing" value={data.urgency} onValueChange={v=>update("urgency",v)} className="timing">{["As soon as possible","This week","Just exploring"].map(v=><label className="choice" key={v}><RadioGroupItem value={v}/>{v}</label>)}</RadioGroup><p className="fine">Concept service area: Northern Virginia. No real availability is implied.</p></>}{step===3&&<><label className="field">Name<input required maxLength={80} autoComplete="name" value={data.name} placeholder="Alex Sample" onChange={e=>update("name",e.target.value)}/></label><div className="field-row"><label className="field">Email<input required type="email" maxLength={160} autoComplete="email" value={data.email} placeholder="alex@example.com" onChange={e=>update("email",e.target.value)}/></label><label className="field">Phone <span>(optional)</span><input type="tel" maxLength={25} autoComplete="tel" value={data.phone} placeholder="(703) 555-0100" onChange={e=>update("phone",e.target.value)}/></label></div><label className="field">Anything else? <span>(optional)</span><textarea maxLength={1000} rows={2} value={data.notes} placeholder="Tell us a little about the issue…" onChange={e=>update("notes",e.target.value)}/></label><label className="consent"><Checkbox checked={consent} onCheckedChange={v=>setConsent(v===true)}/><span>I understand this is a demo and these details will be saved for demonstration purposes.</span></label><p className="fine">{data.service} · {data.zip} · {data.urgency}</p></>}{error&&<p className="error" role="alert">{error}</p>}<div className="form-actions">{step>1&&<button type="button" className="back" onClick={()=>next(step-1)} disabled={busy}>Back</button>}<button type="submit" className="button" disabled={busy}>{busy?"Saving your request…":step===3?"Submit Demo Request":"Continue"}{!busy&&<ArrowRight size={18}/>}</button></div><p className="form-footer">Demo only · No obligation · No real booking</p></form>}</div></section>
 <section id="areas" className="area"><div className="container area-grid"><div><div className="eyebrow light">ROOTED IN NORTHERN VIRGINIA</div><h2>Local comfort.<br/>Close to home.</h2><p>A concept built for the neighborhoods<br/>we call home.</p></div><div><div className="towns">{["Centreville","Fairfax","Chantilly","Herndon","Reston","Vienna","Ashburn","Manassas"].map(t=><span key={t}><MapPin size={16}/>{t}</span>)}</div><p className="fine">Illustrative service area for this fictional company.</p></div></div></section>
 <section id="faq" className="section container faq-grid"><div><div className="eyebrow">A LITTLE CLARITY</div><h2>Good questions.<br/>Simple answers.</h2></div><div className="faqs">{[["Is ComfortPro a real company?","No. ComfortPro is a fictional HVAC and plumbing concept created to demonstrate a home-service website. It does not represent a real provider, license, accreditation, or customer endorsement."],["What happens when I submit the form?","Your demo request is saved and you receive a reference number plus a downloadable confirmation. No email is sent, no appointment is created, and no one will contact you. Please use sample details."],["Does the Call Now button work?","It opens your device’s phone app using (703) 555-0148, a fictional demo number. It does not reach a service business."],["Can I request urgent service?","You can test the urgent-request option, but this demo cannot dispatch help. For a real repair, contact an actual local provider; for an immediate danger, contact emergency services."]].map(([q,a])=><details key={q}><summary>{q}<ChevronDown size={19}/></summary><p>{a}</p></details>)}</div></section>
 </main><footer><div className="container footer-top"><a className="brand" href="#home"><span className="brand-icon"><House size={25}/></span><span>Comfort<span className="orange">Pro</span><small>HVAC & PLUMBING</small></span></a><p>Comfort restored. Problems solved.</p><a href="#estimate">Try the estimate experience <ArrowRight size={17}/></a></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} ComfortPro Concept. Fictional demo.</span><span>No real ratings, reviews, licensing, or accreditation claims.</span></div></footer><div className="mobile-bar"><a href="tel:+17035550148"><Phone size={18}/> Call demo number</a><button className="button" onClick={()=>estimate()}>Free Estimate <ArrowRight size={16}/></button></div></>;
}

